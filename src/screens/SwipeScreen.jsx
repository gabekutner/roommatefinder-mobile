import {
  View,
  Text,
} from 'react-native';



export default function Swipe({ navigation }) {
  return (
    <View style={{ flex:1, justifyContent:'center', alignItems:'center' }}>
      <Text>Swipe</Text>
    </View>
  )
}